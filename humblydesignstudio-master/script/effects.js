$(document).ready(function() {/*
	var $slideWrapper = $('div#slide-wrapper');
	
	// Insert buttons into slide here
	var $slideOneButton = $('<button class="action"><span>The Process &ShortRightArrow;</span></button>');
	$('div#slide-1 div.button-wrapper').append($slideOneButton);
	$slideOneButton.on('click', function() {
		$slideWrapper.css('margin-top', '-377px');
	});
	
	var $slideTwoButton = $('<button class="action"><span>Learn More &ShortRightArrow;</span></button>');
	$('div#slide-2 div.button-wrapper').append($slideTwoButton);
	$slideTwoButton.on('click', function() {
		$slideWrapper.css('margin-top', '-754px');
	});
	
	var $slideThreeButton = $('<a href="contact.html"><button class="action preview-next"><span>Get Quote</span></button></a>');
	$('div#slide-3 div.button-wrapper').append($slideThreeButton);
	*/
});